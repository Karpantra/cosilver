  <%= simple_form_for [flat, offer] do |f| %>
    <%= f.input :content %>
    <%= f.input :title, placeholder: "Address" %>
    <%= f.input :price, placeholder: "City" %>
    <%= f.input :room_size, placeholder: "Service charge"%>
    <%= f.submit "Find a new offer", class: "btn btn-success" %>
  <% end %>
