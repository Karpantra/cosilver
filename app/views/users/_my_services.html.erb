<% @availabilities.each do |availability| %>
<div class="product">
  <% if availability.service.provider.photos? %>
  <%= cl_image_tag availability.service.provider.photos.first.path, width: 100, height: 100, crop: :fill, class: "avatar-large img-thumbnail" %>
  <% else %>
  <%= image_tag "cosilverlogo3.png", class: "product-image" %>
  <% end %>
  <div class='product-body'>
    <h3><%= availability.service.title %></h3>
    <span><strong><i class="fa fa-user" aria-hidden="true"></i> <%= availability.service.provider.first_name %> <%= availability.service.provider.last_name %></strong></span>
    <span>   <i class="fa fa-calendar" aria-hidden="true"></i> <%= availability.date.strftime("%d/%m/%Y") %></span>
    <span>   <i class="fa fa-envelope-o" aria-hidden="true"></i>
<%= availability.service.provider.email  %></span>
  </div>
</div>
<% end %>

